[package]
name = "temp_rust_test"
version = "0.1.1"
edition = "2021"

[dependencies]
# RunAgent Rust SDK from crates.io
runagent = "0.1.33"

# Required dependencies
tokio = { version = "1.0", features = ["full"] }
serde_json = "1.0"
anyhow = "1.0"
futures = "0.3"  